{
  "name": "chess-tent-mnonorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:dev": "lerna exec \"yarn build:dev\"",
    "build": "lerna exec \"yarn build\"",
    "postinstall": "husky install && lerna exec \"yarn install\""
  },
  "devDependencies": {
    "husky": "^5.0.1",
    "lerna": "^3.21.0",
    "prettier": "^2.2.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
