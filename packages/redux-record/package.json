{
  "name": "@chess-tent/redux-record",
  "version": "1.0.0",
  "main": "dist/src/index.js",
  "scripts": {
    "build:dev": "tsc -p tsconfig.build.json",
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "peerDependencies": {
    "react": "*",
    "react-redux": "*",
    "redux": "*"
  },
  "devDependencies": {
    "@types/react": "^16.9.0",
    "@types/react-redux": "^7.1.3",
    "@types/redux": "^3.6.0"
  }
}
