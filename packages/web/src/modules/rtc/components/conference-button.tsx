import styled from '@emotion/styled';

export const ConferenceButton = styled.div({
  '&:hover': {
    opacity: 0.8,
  },
  borderRadius: '100%',
  cursor: 'pointer',
  height: 40,
  paddingBottom: 3,
  width: 40,
});
